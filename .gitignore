# OS
.DS_Store
Thumbs.db

# Secrets — NEVER commit
.env
backend/.env
.turtlecruise_secrets.local.env
*.local.env

# Env templates (must stay in git)
!.env.example
!backend/.env.example

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
.eggs/
/dist/
build/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Virtualenv
.venv/
venv/
backend/.venv/

# Django local DB and collected static
backend/db.sqlite3
backend/*.sqlite3
backend/staticfiles/

# WP media (in git — needed for /media/wp/ after import_wp_site)

# WordPress dump (large, contains customer data)
turtlecruisebyscubacat/
installer.php

# IDE / Cursor (local index cache — never commit)
.idea/
.vscode/
.cursor/
.cursorj/
**/.cursorj/
*.swp
*.swo

# Logs
*.log
logs/

# Docker volumes (local)
pgdata/

# Internal audit / planning notes (may reference secrets)
admin_hidden_settings_audit.md
django_migration_plan.md
